import React from 'react';
import { SB_SECTIONS } from 'const';
import AddPipeline from '.';

export default {
  title: `${SB_SECTIONS.RIGHT}|Add Pipeline`,
};

export const Default = () => <AddPipeline />;
