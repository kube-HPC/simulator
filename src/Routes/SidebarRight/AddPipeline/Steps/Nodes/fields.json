{
  "init": {
    "name": "string",
    "flowInput": {},
    "description": "string",
    "kind": "batch", // batch, stream
    "experimentName": "main" // implicit?
  },
  "nodes": {
    "common": {
      "nodeName": "string" // Done
    },
    "algorithms": {
      "algorithmName": "string", // Done
      "pipelineName": "string", // Done
      "input": [], // Done
      "ttl": 0, // Done
      "includeInResult": true, // Done
      "metrics": {
        "tensorboard": true // Done
      },
      "retry": {
        "policy": "OnCrash", // Done
        "limit": 1 // Done
      },
      "batchOperation": "indexed", // Done // indexed, cartesian
      "stateType": "stateless", // Done // stateless, stateful
      "kind": "dataSource"
    },
    "dataSource": {
      "dataSource": {
        "id": "string",
        "name": "string", // if no id,
        "snapshotName": "string" // if no id
      }
    }
  },
  "options": {
    "webhooks": {
      "progress": "http://my-url-to-progress",
      "result": "http://my-url-to-result"
    },
    "priority": 3,
    "triggers": {
      "pipelines": ["string"],
      "cron": {
        "pattern": "string",
        "enabled": false
      }
    },
    "tags": ["string"],
    "rootJobId": "string",
    // only if kind streaming
    "streaming": {
      "defaultFlow": "string",
      "flows": {
        "main": "A >> B&C , B >> D",
        "second": "A >> B >> C"
      }
    }
  }
}
// "options": {},
// "edges": [
//   {
//     "source": "A",
//     "target": "B"
//   }
// ]
