import React from 'react';
import { SB_SECTIONS } from 'const';
import AddDebugReact from './AddDebug.react';

export default {
  title: `${SB_SECTIONS.RIGHT}|Add Debug`,
};

export const Default = () => <AddDebugReact />;
