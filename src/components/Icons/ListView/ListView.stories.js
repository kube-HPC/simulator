import React from 'react';
import { SB_SECTIONS } from 'const';
import { IconListView } from '..';

export default {
  title: `${SB_SECTIONS.ICONS}List View`,
};

export const Default = () => <IconListView />;
