import React from 'react';
import { SB_SECTIONS } from 'const';
import { IconCardView } from '..';

export default {
  title: `${SB_SECTIONS.ICONS}Card View`,
};

export const Default = () => <IconCardView />;
